$(function(){
	$(".new_message_form").html("<%= escape_javascript render 'messages/message_form' %>");
	var message_dialog = $(".dialog_new_message").dialog({
		modal: true,
		width: 450,
		resizable: false
	});
	$("#recipient_name").message_autocomplete();

});