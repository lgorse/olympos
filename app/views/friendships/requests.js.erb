$(function(){
	$(".pending_friendship_list").html("<%= escape_javascript(render 'users/mini_summary_list', :friendship_list => @friend_requests, :empty_prompt => 'No friendship requests sent') %>");
});
