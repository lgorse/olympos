<%= form_for(@current_user.reverse_friendships.build(:friender_id => user.id), :url => accept_friendships_path, :remote => true) do |f| %>
	<%= f.hidden_field :friender_id %>
			<%= f.submit "Accept" %>
	<% end %>