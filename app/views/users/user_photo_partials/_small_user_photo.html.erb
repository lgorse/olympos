<% if user.facebook? %>
<%= link_to (image_tag user.fb_pic_square, :class => "my_profile_button round", :size => "30x30") + name, user %>
<% else %>
<%= link_to image_tag(user.photo.url(:square), :class => "my_profile_button round", :height => "30") + name, user %>
<% end %>