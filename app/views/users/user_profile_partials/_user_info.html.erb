<div class = "main_user_name">
	<%= @full_name %>
</div>
<% if current_user?(@user) && @user.first_rating.blank? %>
	<%= form_for @user do |user| %>
		<%= render 'layouts/ratings/first_rating', :user => user %>
	<% end %>
<% else %>
	<%= render 'layouts/ratings/user_rating', :star_size => "stars", :user => @user %>
<% end %>

